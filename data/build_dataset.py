"""Compatibility wrapper for the packaged dataset builder.

Install the package with `pip install -e .`, then prefer `resumeai-build-dataset`.
"""

from resumeai.build_dataset import main


if __name__ == "__main__":
    main()
